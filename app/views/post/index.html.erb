<h1>Posts</h1>

<% if user_signed_in? %>
  <%= link_to 'New Post', new_post_path %>
<% end %>

<%= @posts.each do |post| %>
  <div>
    <p><%= post.body %></p>
    <% if user_signed_in? %>
      <p><strong>Author:</strong> <%= post.user.username %></p>
    <% end %>
  </div>
<% end %>